{
  "name": "plugin-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-zip.js",
    "build:main": "tsc -p tsconfig.json",
    "build:renderer": "esbuild src/renderer.ts --bundle --platform=browser --format=cjs --outfile=dist/renderer.js"
  },
  "devDependencies": {
    "adm-zip": "^0.5.17",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
